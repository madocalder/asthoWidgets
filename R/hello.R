#' Temporary function, until we have some real widgets in the package
#'
#' @param   x   Scalar character. A name to be greeted.
#' @return   NULL
#' @export

hello <- function(x = "World") {
  message("Hello, ", x, "!")
}
