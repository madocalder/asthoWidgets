% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts.R
\name{create_base_chart}
\alias{create_base_chart}
\title{Create an empty themed Highchart into which a single chart series can be added}
\usage{
create_base_chart(type, hc_thm = get_astho_hc_theme(), export_options = list())
}
\arguments{
\item{type}{Highcharts chart type, e.g. \code{"pie"}, \code{"column"},
\code{"bar"}, \code{"line"}, \code{"bubble"}, \code{"wordcloud"}.}

\item{hc_thm}{Highchart theme object. Defaults to
\code{get_astho_hc_theme()} so charts pick up the ASTHO brand styling
without the caller having to wire it up.}

\item{export_options}{List. Options forwarded to \code{viz_export()}
for the chart's PNG / SVG / CSV / XLS download menu.}
}
\value{
A \code{highchart} htmlwidget.
}
\description{
Mirrors \code{create_base_map()} but for non-map chart types (column, bar,
pie, line, etc.). Use the returned object as the first argument to one of
the \code{add_*_chart()} helpers.
}
