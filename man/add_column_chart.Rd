% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts.R
\name{add_column_chart}
\alias{add_column_chart}
\title{Add a column / bar series to a base chart}
\usage{
add_column_chart(
  hc,
  data,
  x_col,
  y_col,
  group_col = NULL,
  column_options = list(),
  xAxis_options = list(),
  yAxis_options = list(),
  title_options = list(),
  subtitle_options = list(),
  tooltip_options = list(),
  legend_options = list(),
  caption_options = list()
)
}
\arguments{
\item{hc}{A \code{highchart} object, typically from
\code{create_base_chart(type = "column")}.}

\item{data}{A data.frame with at least \code{x_col} and \code{y_col};
include \code{group_col} for grouped/stacked layouts. Extra columns
stay accessible to tooltip templates as \code{point.<column>}.}

\item{x_col}{Column name (string) for the x-axis category.}

\item{y_col}{Column name (string) for the y-axis value.}

\item{group_col}{Optional column name. When supplied, data is split
into one Highcharts series per unique value.}

\item{column_options}{Optional. Recognised entries: \code{inverted}
(default \code{FALSE}; renders horizontal bars), \code{stacking}
(\code{"normal"}, \code{"percent"}, or \code{NULL}), \code{pointWidth},
\code{dataLabels} (full Highcharts dataLabels list).}

\item{xAxis_options}{Optional: \code{title}, \code{categories}.
\code{categories} defaults to \code{unique(data[[x_col]])}.}

\item{yAxis_options}{Optional: \code{title}, \code{min} (default
\code{0}), \code{max}, \code{labels} (full Highcharts labels list).}

\item{title_options}{Essential: \code{title}. Optional: \code{margin},
\code{widthAdjust}.}

\item{subtitle_options}{Optional: \code{subtitle}, \code{useHTML},
\code{verticalAlign}, \code{x}, \code{y}.}

\item{tooltip_options}{Optional: \code{pointFormat},
\code{headerFormat}, \code{useHTML}, \code{shared}.}

\item{legend_options}{Optional: \code{enabled} (default \code{TRUE}
when \code{group_col} is set, otherwise \code{FALSE}), \code{layout},
\code{align}, \code{verticalAlign}, \code{titleText}.}

\item{caption_options}{Optional: \code{text}, \code{useHTML},
\code{margin}.}
}
\value{
A \code{highchart} object.
}
\description{
Handles single-series, multi-series grouped, stacked, and horizontal
(\code{column_options$inverted = TRUE}, which is what Highcharts calls a
bar chart) variants. When \code{group_col} is supplied, one series is
rendered per unique value, coloured from the chart's theme palette.
}
