% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart_sankey.R
\name{add_sankey_chart}
\alias{add_sankey_chart}
\title{Add a Sankey series to a base chart}
\usage{
add_sankey_chart(
  hc,
  data,
  from_col,
  to_col,
  weight_col,
  sankey_options = list(),
  title_options = list(),
  subtitle_options = list(),
  tooltip_options = list(),
  legend_options = list(),
  caption_options = list()
)
}
\arguments{
\item{hc}{A \code{highchart} object, typically from
\code{create_base_chart(type = "sankey")}.}

\item{data}{A data.frame with at least \code{from_col},
\code{to_col}, and \code{weight_col}.}

\item{from_col}{Column name (string) for the source node.}

\item{to_col}{Column name (string) for the target node.}

\item{weight_col}{Column name (string) for the flow weight.}

\item{sankey_options}{Optional. Recognised entries:
\code{nodeWidth} (default \code{20}), \code{nodePadding}
(default \code{10}), \code{linkOpacity} (default \code{0.5}),
\code{dataLabels} (full Highcharts dataLabels list).}

\item{title_options}{Essential: \code{title}.}

\item{subtitle_options}{Optional: \code{subtitle}.}

\item{tooltip_options}{Optional: \code{nodeFormat}, \code{pointFormat}.}

\item{legend_options}{Optional. Sankeys do not normally render a
legend, but the option is accepted for parity with other widgets.}

\item{caption_options}{Optional: \code{text}, \code{useHTML},
\code{margin}.}
}
\value{
A \code{highchart} object.
}
\description{
Each row of \code{data} becomes one Sankey flow: a link from
\code{from_col} to \code{to_col} with a weight from \code{weight_col}.
Node colors come from the theme palette.
}
