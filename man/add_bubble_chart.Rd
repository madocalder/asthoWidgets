% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart_bubble.R
\name{add_bubble_chart}
\alias{add_bubble_chart}
\title{Add a bubble series to a base chart}
\usage{
add_bubble_chart(
  hc,
  data,
  x_col,
  y_col,
  z_col = NULL,
  group_col = NULL,
  bubble_options = list(),
  xAxis_options = list(),
  yAxis_options = list(),
  title_options = list(),
  subtitle_options = list(),
  tooltip_options = list(),
  legend_options = list(),
  caption_options = list()
)
}
\arguments{
\item{hc}{A \code{highchart} object, typically from
\code{create_base_chart(type = "bubble")}.}

\item{data}{A data.frame with at least \code{x_col} and
\code{y_col}.}

\item{x_col, y_col}{Column names (strings) for the bubble's
horizontal and vertical position.}

\item{z_col}{Optional column name for the bubble size. Defaults
to \code{y_col} so the largest values become the largest bubbles.}

\item{group_col}{Optional column name. When supplied, one series
is rendered per unique value.}

\item{bubble_options}{Optional. Recognised entries: \code{minSize}
(default \code{"15"}), \code{maxSize} (default \code{"60"}),
\code{dataLabels} (full Highcharts dataLabels list).}

\item{xAxis_options, yAxis_options, title_options, subtitle_options, tooltip_options, legend_options, caption_options}{Same shape as \code{add_column_chart()}.}
}
\value{
A \code{highchart} object.
}
\description{
Each row becomes one bubble. \code{x_col} and \code{y_col} position
the bubble; \code{z_col} sets the bubble size (defaults to
\code{y_col} if not supplied). Supplying \code{group_col} splits the
data into one series per unique value, so the color palette can
encode a categorical variable.
}
